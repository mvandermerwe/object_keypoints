import setuptools

setuptools.setup(
    name="object_keypoints",
    version="0.0.1",
    packages=["object_keypoints"],
    url="",
    description="Unsupervised Learning of 3D Landmarks via Reconstruction",
    install_requires=[]
)
